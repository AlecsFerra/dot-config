(defpoll bluetooth-connected
  :initial  false
  :interval "5s"
  :run-while show-extra
  "extra/bluetooth/bluetooth.sh info")

(defwidget bluetooth []
  (button
    :onclick "extra/bluetooth/bluetooth.sh"
    :class   "bluetooth"
    { bluetooth-connected ? "" : "" }))
