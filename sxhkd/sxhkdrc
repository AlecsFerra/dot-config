# vim: filetype=sh

super + {_, shift} + Return
    $TERMINAL { , --class dropdown_terminal}

super + w
    firefox

super + w + shift
    tor-browser

Print
    $HOME/.config/sxhkd/scripts/scrot.sh

super + d
    rofi -show drun -modi drun -config $HOME/.config/rofi/config

super + e
    $HOME/.config/rofi/scripts/emoji/emoji-selector.sh

super + g
    $HOME/.config/rofi/scripts/search/search.sh

super + m
    $HOME/.config/scripts/monitor.sh && $HOME/.config/polybar/start.sh

super + shift + f
    $TERMINAL -e zsh -ic ranger

super + shift + x
    sleep 1 && xset dpms force suspend

XF86AudioMute
    pavolume mutetoggle --noshow

XF86AudioRaiseVolume
    pavolume volup 5 --noshow

XF86AudioLowerVolume
    pavolume voldown 5 --noshow

XF86MonBrightnessUp
    xbacklight -inc 5

XF86MonBrightnessDown
    xbacklight -dec 5

## BSPWM specific ##
super + {_,shift} + q
    bspc node -{c,k}

super + {_,shift} + {h,j,k,l}
    bspc node -{f,s} {west,south,north,east}

super + ctrl + {h,j,k,l,space}
    bspc node -p {west,south,north,east,cancel}

super + {_,shift} + {1-9,0}
    bspc {desktop -f,node -d} '^{1-9,10}'

super + {t, s, f}
    bspc node -t {tiled, floating, fullscreen}
